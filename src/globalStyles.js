import { StyleSheet } from "react-native";
import { textBase } from "./constants";

export default globalStyles = StyleSheet.create({
  defaultText: {
    color: textBase,
    // fontSize: 20,
    fontFamily: "JetBrains-Mono-bold",
  },
});
